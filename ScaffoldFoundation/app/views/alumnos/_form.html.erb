<%= form_for(alumno) do |f| %>
  <% if alumno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alumno.errors.count, "error") %> prohibited this alumno from being saved:</h2>

      <ul>
      <% alumno.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :numero_control %>
    <%= f.number_field :numero_control %>
  </div>

  <div class="field">
    <%= f.label :carrera %>
    <%= f.text_field :carrera %>
  </div>

  <div class="field">
    <%= f.label :anio_ingreso %>
    <%= f.date_select :anio_ingreso %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
